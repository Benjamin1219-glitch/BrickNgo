<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Contact | BRICKNGO</title>

  <!-- Fonts & Icons -->
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>

  <style>
    :root {
      --orange: #ff9800;
      --blue-dark: #06204a;
      --blue-light: #4da6ff;
      --glass-dark: rgba(6, 32, 68, 0.7);
    }

    body{
      font-family:'Montserrat',sans-serif;
      background:linear-gradient(120deg,#06204a,#0b3b91,#4da6ff,#ffd49a,#ff9800);
      background-size:200% 200%;
      animation:flow 20s linear infinite;
      color:#0f2940;
      margin:0;
      padding:0;
    }

    @keyframes flow{
      0%{background-position:0% 50%}
      50%{background-position:100% 50%}
      100%{background-position:0% 50%}
    }

    /* ===== LOGO & NAVBAR ===== */
    .brand {
      text-align: center;
      padding-top: 25px;
    }

    .brand img {
      width: 160px;
      height: auto;
      filter: drop-shadow(0 6px 10px rgba(0,0,0,0.2));
    }

    nav.topnav {
      position: sticky;
      top: 10px;
      z-index: 10;
      display: flex;
      justify-content: center;
      background: var(--glass-dark);
      backdrop-filter: blur(8px);
      -webkit-backdrop-filter: blur(8px);
      border-radius: 12px;
      margin: 20px auto;
      padding: 10px 25px;
      width: fit-content;
      box-shadow: 0 8px 25px rgba(0,0,0,0.25);
    }

    nav.topnav .navlinks {
      display: flex;
      gap: 20px;
    }

    nav.topnav a {
      color: #fff;
      text-decoration: none;
      padding: 8px 14px;
      border-radius: 10px;
      font-weight: 600;
      letter-spacing: 0.4px;
      transition: all 0.3s ease;
    }

    nav.topnav a:hover {
      background: linear-gradient(90deg, var(--orange), #ffb74d);
      color: #062238;
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(255,152,0,0.3);
    }

    /* ===== CONTACT SECTION ===== */
    .contact-section{
      max-width: 750px;
      margin: 60px auto;
      background: rgba(255,255,255,0.95);
      border-radius: 16px;
      box-shadow: 0 10px 40px rgba(0,0,0,0.2);
      padding: 40px;
      text-align: center;
    }

    .contact-section h1{
      color:#d35400;
      margin-bottom:10px;
    }

    form{
      display:flex;
      flex-direction:column;
      gap:15px;
      margin-top:20px;
    }

    input,textarea{
      padding:12px 15px;
      border-radius:10px;
      border:1px solid rgba(6,32,68,0.1);
      font-family:'Montserrat';
      font-size:1rem;
      resize:none;
    }

    button{
      background:linear-gradient(90deg,#ff9800,#ffb74d);
      border:none;
      color:#062238;
      padding:12px;
      border-radius:10px;
      font-weight:700;
      cursor:pointer;
      box-shadow:0 10px 25px rgba(255,152,0,0.2);
      transition:0.3s;
    }

    button:hover{
      transform:translateY(-3px);
      box-shadow:0 15px 30px rgba(255,152,0,0.3);
    }

    /* ===== INFO CARDS ===== */
    .info-grid{
      display:grid;
      grid-template-columns:repeat(auto-fit,minmax(240px,1fr));
      gap:20px;
      margin-top:40px;
    }

    .info-card{
      background:rgba(255,255,255,0.9);
      border-radius:12px;
      padding:20px;
      box-shadow:0 8px 24px rgba(3,12,35,0.1);
      text-align:center;
      transition:0.3s;
    }

    .info-card:hover{
      transform:translateY(-5px);
      box-shadow:0 12px 30px rgba(3,12,35,0.2);
    }

    .info-card i{
      font-size:2rem;
      color:#ff9800;
      margin-bottom:10px;
    }

    .info-card h4{
      color:#062238;
      margin-bottom:8px;
      font-size:1.1rem;
    }

    .info-card p{
      color:#0f2940;
      font-weight:500;
      font-size:0.95rem;
    }

    /* ===== RESPONSIVE ===== */
    @media (max-width:768px){
      nav.topnav {
        width: 90%;
      }
      .contact-section{
        padding:25px;
        margin:40px 10px;
      }
    }
  </style>
</head>

<body>
  <!-- Logo -->
  <header class="brand">
    <img src="/images/brickngo_logo.jpg" alt="BRICKNGO Logo">
  </header>

  <!-- Navigation -->
  <nav class="topnav">
    <div class="navlinks">
      <a href="/">Home</a>
      <a href="/products">Products</a>
      <a href="/about">About</a>
      <a href="/contact">Contact</a>
    </div>
  </nav>

  <!-- Contact Section -->
  <section class="contact-section">
    <h1>Contact Us</h1>
    <p>Fill out the form below — your message will be sent directly to our WhatsApp chat.</p>

    <!-- WhatsApp Form -->
    <form id="contactForm">
      <input type="text" id="name" placeholder="Your Name" required />
      <input type="text" id="phone" placeholder="Your Phone" required />
      <textarea id="message" placeholder="Your Message" rows="5" required></textarea>
      <button type="button" onclick="sendWhatsApp()">Send WhatsApp Message</button>
    </form>

    <!-- Contact Info Cards -->
    <div class="info-grid">
      <div class="info-card">
        <i class="fas fa-phone"></i>
        <h4>Call Us</h4>
        <p>+91 96181 99232</p>
      </div>

      <div class="info-card">
        <i class="fas fa-envelope"></i>
        <h4>Email</h4>
        <p>info@brickngo.com</p>
      </div>

      <div class="info-card">
        <i class="fas fa-location-dot"></i>
        <h4>Office Address</h4>
        <p>Hyderabad, Telangana, India</p>
      </div>

      <div class="info-card">
        <i class="fab fa-whatsapp"></i>
        <h4>WhatsApp</h4>
        <p>
          <a href="https://wa.me/918019447598"
             target="_blank"
             style="color:#1766d1;text-decoration:none;">
             Chat Now
          </a>
        </p>
      </div>
    </div>
  </section>

  <script>
    function sendWhatsApp() {
      const name = document.getElementById("name").value.trim();
      const phone = document.getElementById("phone").value.trim();
      const message = document.getElementById("message").value.trim();

      const businessNumber = "919618199232";

      if (!name || !phone || !message) {
        alert("Please fill out all fields before sending.");
        return;
      }

      const text = `🧱 *New Customer Inquiry from BRICKNGO Website* %0A%0A👤 *Name:* ${name}%0A📞 *Phone:* ${phone}%0A💬 *Message:* ${message}%0A%0AThank you for contacting us!`;
      const url = `https://wa.me/${businessNumber}?text=${text}`;
      window.open(url, "_blank");
    }
  </script>
</body>
</html>
